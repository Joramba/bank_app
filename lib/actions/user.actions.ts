"use server";

export const signIn = async () => {
  try {
    // Mutation
  } catch (error) {
    console.log(error);
  }
};

export const signUp = async (userData: SignUpParams) => {
  try {
    // Create user account
  } catch (error) {
    console.log(error);
  }
};
